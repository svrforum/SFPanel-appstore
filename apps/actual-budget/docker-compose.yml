services:
  actual:
    image: actualbudget/actual-server:latest
    container_name: actual-budget
    restart: unless-stopped
    ports:
      - "${PORT:-5006}:5006"
    volumes:
      - actual-data:/data

volumes:
  actual-data:
