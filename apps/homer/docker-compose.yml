services:
  homer:
    image: b4bz/homer:latest
    container_name: homer
    restart: unless-stopped
    ports:
      - "${PORT:-8088}:8080"
    volumes:
      - homer-assets:/www/assets

volumes:
  homer-assets:
